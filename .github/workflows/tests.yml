name: GitHub CI

on:
  pull_request:
    branches: [ master ]
    types: [ opened, synchronize, reopened, labeled ]
  schedule:
  - cron:  '0 0 * * *'
    
env:
  COMPILE_JOBS: 2

jobs:
  build:
    if: ${{ github.event_name == 'schedule' || contains(github.event.pull_request.labels.*.name, 'ready to test') }}
    name: Build ${{ matrix.build_type }}-dealii:${{ matrix.dealii_version }}-${{ matrix.adaflo }}
    runs-on: self-hosted

    strategy:
      fail-fast: false
      matrix:
        build_type: ["Release"]
        dealii_version: ["master"]
        adaflo: ["adaflo=on"]
        exclude:
          - build_type: "Release"
            adaflo: "adaflo=off"
    
    container:
      image: dealii/dealii:${{ matrix.dealii_version }}-focal

    steps:
      - name: Setup
        run: |
          sudo chown -R $USER:$USER $GITHUB_WORKSPACE
          echo "${{ toJson(github.event.pull_request.labels.*.name) }}"
          
      - uses: actions/checkout@v2

      - name: Compile adaflo
        if: matrix.adaflo == 'adaflo=on'
        run: |
          git clone https://github.com/kronbichler/adaflo.git
          mkdir adaflo-build
          cd adaflo-build
          cmake \
            -D BUILD_SHARED_LIBS=ON \
            -D CMAKE_BUILD_TYPE=${{ matrix.build_type }} \
            ../adaflo
          make -j${{ env.COMPILE_JOBS }} adaflo

      - name: Compile MeltPoolDG
        run: |
          mkdir build
          cd build
          cmake \
            -D CMAKE_CXX_FLAGS="-Werror" \
            -D CMAKE_BUILD_TYPE=${{ matrix.build_type }} \
            -D ADAFLO_LIB=/__w/MeltPoolDG/MeltPoolDG/adaflo-build/ \
            -D ADAFLO_INCLUDE=/__w/MeltPoolDG/MeltPoolDG/adaflo/include/ \
            .. 
          make -j${{ env.COMPILE_JOBS }}
          
      - name: Test
        run: |
          cd build
          ctest -V -R 'unit_tests/|tests/'
